/* Convet the string "123" to 123 without using built in api*/
#include<iostream>
#include<string>
using namespace std;

int main()
{  
    string str;
    int output_num;
    str.length();
    for(int i=0;i<str.length();i++)
    {
        if (str[i]== char)
            output_num= output_num*10 + i;
    }    
}